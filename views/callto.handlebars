<div class="container card callto">
    <div class="content">
        <h1>Stay updated!</h1>
        <p>Join 60,000+ product managers receiving monthly updates on:</p>
        <ul>
            <li>Product discovery and building what matters</li>
            <li>Measuring to ensure updates are a success</li>
            <li>And much more!</li>
        </ul>
        <form class="container submission" action="/" method="post">
            <div class="container">
                <label for="email"><b>Email Address</b></label>
                <p {{#if failed}} class="{{invalid}}" {{else}} class="invis" {{/if}}>Valid email required</p>
            </div>
            <input {{#if failed}} class="{{invalid}}" {{/if}} type="email" name="email" id="email" placeholder="email@company.com">
            <button type="submit">Subscribe to monthly newsletter</button>
        </form>
    </div>
    <img id="desktop-img" src="/assets/images/illustration-sign-up-desktop.svg">
    <img id="mobile-img" src="/assets/images/illustration-sign-up-mobile.svg">
</div>